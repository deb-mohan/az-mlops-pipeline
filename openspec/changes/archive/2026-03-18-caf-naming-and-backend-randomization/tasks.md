# Implementation Tasks

## 1. CAF Resource Naming in Terraform

- [x] 1.1 Update `locals` block in `infrastructure/terraform/main.tf` to use CAF prefix pattern
  - [x] 1.1.1 Change `resource_group_name` to `rg-{project}-{env}`
  - [x] 1.1.2 Change `ml_workspace_name` to `mlw-{project}-{env}`
  - [x] 1.1.3 Change `storage_account_name` to use `st` prefix instead of `sa` suffix
- [x] 1.2 Update inline resource names in `infrastructure/terraform/main.tf`
  - [x] 1.2.1 Change Application Insights name to `appi-{project}-{env}`
  - [x] 1.2.2 Change Key Vault name to `kv-{project}-{env}-{suffix}`

## 2. Bootstrap Backend Randomization

- [x] 2.1 Add backend.tf detection and parsing logic to `scripts/bootstrap-backend.sh`
  - [x] 2.1.1 Add `BACKEND_TF` path constant pointing to `infrastructure/terraform/backend.tf`
  - [x] 2.1.2 Add logic to parse `storage_account_name` from existing `backend.tf`
  - [x] 2.1.3 Add logic to parse `resource_group_name` from existing `backend.tf`
- [x] 2.2 Add random name generation when no `backend.tf` exists
  - [x] 2.2.1 Generate storage account name using `tfstate$(date +%s)` pattern
  - [x] 2.2.2 Set resource group name to `rg-tfstate-iemlops-<location>`
- [x] 2.3 Add `backend.tf` generation after successful resource creation
  - [x] 2.3.1 Write `backend.tf` with resource group, storage account, container, and `use_azuread_auth`
  - [x] 2.3.2 Only generate when `backend.tf` did not previously exist
- [x] 2.4 Update resource group name default from `terraform-state-rg` to `rg-tfstate-iemlops-<location>`
- [x] 2.5 Remove `.env.local` generation block from `scripts/bootstrap-backend.sh`
  - [x] 2.5.1 Remove the `cat > .env.local` heredoc block
  - [x] 2.5.2 Remove the `sed` replacement commands for `.env.local`
  - [x] 2.5.3 Remove the `.env.local` success message
- [x] 2.6 Update completion summary to show whether `backend.tf` was generated or reused

## 3. Documentation Updates

- [x] 3.1 Update Resource Naming Convention section in `README.md`
  - [x] 3.1.1 Change pattern description to `{type}-{project}-{environment}`
  - [x] 3.1.2 Update example resource names to CAF-aligned versions
- [x] 3.2 Update Backend Configuration section in `README.md`
  - [x] 3.2.1 Document that `backend.tf` is generated by bootstrap
  - [x] 3.2.2 Update resource group name to `rg-tfstate-iemlops-<location>`
  - [x] 3.2.3 Document random storage account naming pattern
  - [x] 3.2.4 Remove references to `.env.local` containing backend metadata
